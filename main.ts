let weightInkg =70; //your current weight in kgs

let heightInmeter =1.75; //your current height in meter

let bmi = weightInkg / (heightInmeter * heightInmeter); // formula of bmi calcultor

console.log(`Your BMI is ${bmi}`); //result

